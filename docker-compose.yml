version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: airflow-postgres
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U airflow"]
      interval: 10s
      timeout: 5s
      retries: 5

  airflow-init:
    build:
      context: .
      dockerfile: docker/Dockerfile.airflow
    container_name: airflow-init
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - AIRFLOW_HOME=/home/airflow
      - AIRFLOW__CORE__DAGS_FOLDER=/home/airflow/dags
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      - AIRFLOW__CORE__LOAD_EXAMPLES=false
      - AIRFLOW__CORE__LOAD_DEFAULT_CONNECTIONS=false
      - _PIP_ADDITIONAL_REQUIREMENTS=pdfkit requests pandas beautifulsoup4 openpyxl plotly streamlit pillow
    command: >
      bash -c "airflow db upgrade &&
               airflow users create --firstname admin --lastname user --username admin --role Admin --email admin@example.com --password admin || true"
    volumes:
      - airflow_home:/home/airflow
      - ./dags:/home/airflow/dags
      - ./scripts:/home/airflow/scripts
      - ./assets:/home/airflow/assets
      - ./output:/home/airflow/output
    user: "0:0"

  airflow-scheduler:
    build:
      context: .
      dockerfile: docker/Dockerfile.airflow
    container_name: airflow-scheduler
    depends_on:
      - airflow-init
      - postgres
    environment:
      - AIRFLOW_HOME=/home/airflow
      - AIRFLOW__CORE__DAGS_FOLDER=/home/airflow/dags
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      - AIRFLOW__CORE__LOAD_EXAMPLES=false
      - AIRFLOW__CORE__LOAD_DEFAULT_CONNECTIONS=false
      - _PIP_ADDITIONAL_REQUIREMENTS=pdfkit requests pandas beautifulsoup4 openpyxl plotly streamlit pillow
    command: airflow scheduler
    volumes:
      - airflow_home:/home/airflow
      - ./dags:/home/airflow/dags
      - ./scripts:/home/airflow/scripts
      - ./assets:/home/airflow/assets
      - ./output:/home/airflow/output
      - ./logs:/home/airflow/logs
    restart: always

  airflow-webserver:
    build:
      context: .
      dockerfile: docker/Dockerfile.airflow
    container_name: airflow-webserver
    depends_on:
      - airflow-init
      - postgres
    environment:
      - AIRFLOW_HOME=/home/airflow
      - AIRFLOW__CORE__DAGS_FOLDER=/home/airflow/dags
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      - AIRFLOW__CORE__LOAD_EXAMPLES=false
      - AIRFLOW__CORE__LOAD_DEFAULT_CONNECTIONS=false
      - _PIP_ADDITIONAL_REQUIREMENTS=pdfkit requests pandas beautifulsoup4 openpyxl plotly streamlit pillow
    command: airflow webserver -p 8080
    volumes:
      - airflow_home:/home/airflow
      - ./dags:/home/airflow/dags
      - ./scripts:/home/airflow/scripts
      - ./assets:/home/airflow/assets
      - ./output:/home/airflow/output
      - ./logs:/home/airflow/logs
    ports:
      - "8080:8080"
    restart: always

volumes:
  postgres_data:
  airflow_home:

networks:
  default:
    name: airflow-network
